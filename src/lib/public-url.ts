/**
 * The published app URL used for all public-facing links
 * (forms, signatures, EPKs, shared files, etc.)
 * 
 * Using the published URL ensures these links work without
 * requiring Lovable authentication, unlike preview URLs.
 */
export const PUBLIC_APP_URL = 'https://artist-manager-connect.lovable.app';
